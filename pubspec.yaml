name: todo_txt
description: Implementation of todo.txt standard for storing todos in plain *.txt files.
version: 1.0.0
repository: https://github.com/galets/todo_txt

environment:
  sdk: ">=2.17.6 <4.0.0"

# Add regular dependencies here.
dependencies:
  meta: ^1.9.1
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
